# 实施计划 —— P5 统计

- Date: `2026-09-10`
- Design Spec: [`docs/aegis/specs/2026-09-10-mental-math-app-design.md`](../specs/2026-09-10-mental-math-app-design.md) §8
- Baseline: [`docs/aegis/baseline/2026-09-10-initial-baseline.md`](../baseline/2026-09-10-initial-baseline.md)
- 上游：[`evidence/p4/ACCEPTANCE.md`](../work/2026-09-10-mental-math-app/evidence/p4/ACCEPTANCE.md)
- 覆盖阶段：规格 §16 的 **P5 统计**
- 后续：P6 分享订阅与退订 → P7 CI 与交付

---

## Goal

交付功能 4：按项目查看日 / 周 / 月折线统计图，以及每轮的 8 项指标
（耗时与正确率的最高 / 最低 / 平均 / 中位）。

**本计划不含**：跨项目汇总、导出、时间范围选择器、订阅相关（P6）。

## Architecture

```
backend/internal/stats/     统计聚合的唯一 owner（纯计算，零外部依赖）
  bucket.go                 三种粒度的分桶与排序
  metrics.go                8 项指标（含中位数）与聚合
  stats.go                  Compute：轮次 -> 分桶 + overall
  stats_test.go

backend/internal/service/stats.go   访问权判定 + 取数 + 调用 stats
backend/internal/api/stats.go        GET /api/projects/:id/stats

app/lib/api/stats_api.dart          端点封装与模型
app/lib/state/stats.dart            统计页状态
app/lib/ui/stats_tab.dart           统计页（替换 P4 的占位）
app/lib/ui/widgets/line_chart.dart  自绘折线图（CustomPainter）
```

依赖方向：`api → service → {store, stats}`。`stats` **不依赖本仓库其他包**，
只接受一个轻量的输入结构，因此可独立测试且未来可被其它调用方复用。

## Tech Stack

无新增依赖。折线图用 `CustomPainter` 自绘（P4 已明确拒绝引入图表库）。

## Baseline / Authority Refs

- 规格 §8.1（计算方式：不物化、三种粒度、每桶 8 项指标）
- 规格 §8.2（UI 分组：两个分段控件、单条线、两张卡）
- 规格 §4.3（统计 Tab **不含任何编辑入口**）
- 规格 §6.1(5)（统计一律基于 `server_is_correct`——本阶段取用的是服务端派生的
  `correct_count`，它已由 P3 按该规则写入）
- 规格 §5.5.3 的**例外说明**：见下方「关于浮点的边界」
- 基线 §5.2(4)（统计不得物化）、§9(4)

### 关于浮点的边界（重要，避免后续误判）

规格 §5.5.3 禁止判分路径出现浮点，理由是**跨端精确性**：Go 与 Dart 必须
逐例一致。统计**不满足**该前提：

- 统计只在服务端计算，**没有跨端契约**——客户端只是渲染返回值；
- 中位数、平均正确率本质是除法，用有理数表示只会增加复杂度而无收益。

因此统计允许使用 `float64`，但必须在代码中写明这一区分，
否则后来者会以为「本项目禁止浮点」而在统计里硬造有理数。

## Compatibility Boundary

1. **统计是派生态**（基线 §5.2(4)）：不得新增聚合表或缓存表。
   删除 `practice_round`/`attempt` 后统计自然消失（功能8 的级联已由外键保证）。
2. **统计 Tab 不得出现编辑入口**（规格 §4.3）。
3. **只能看自己的数据**：统计按 `(user_id, project_id)` 过滤。
   作者看不到订阅者的统计，订阅者也看不到作者的——这是隐私边界，
   不是权限细节。
4. 不得改动既有 `practice_round` 的写入语义。

## TDD Route

```text
TDD Route:
- Mode: off
- Decision: skipped
- Strict authority: not applicable
- Test posture: post-change regression
- Reason: 项目配置 tdd_mode = "off"，用户未要求严格 TDD
- Verification: go test ./... -count=1 ; flutter analyze ; flutter test
```

> 中位数与分桶是**最易算错**的部分（A8 专门点名），故为核心算法写表驱动测试，
> 含奇偶长度、单元素、空桶、跨月跨年、时区偏移等边界。

## Verification

```powershell
cd D:\Desktop\Cala\backend ; go build ./... ; go vet ./... ; go test ./... -count=1
cd D:\Desktop\Cala\app     ; flutter analyze ; flutter test
```

端到端：起后端，注册 → 建项目 → 做 3 轮（不同答对率）→ 取三种粒度的统计。

---

## Scope Check

**Aegis Visibility**：本阶段落地的是**由历史事实推导出的数字**。它与答题记录之间
没有冗余存储，因此「统计是否正确」完全取决于聚合算法本身——一旦算错，
没有任何第二份数据可以对照发现。这就是为中位数与分桶写表驱动测试的原因。

```text
Requirement Ready Check:
- Requirement source refs: 用户功能 4；规格 §8
- Goals and scope refs: 规格 §16 P5 行
- User / scenario refs: 已登录用户查看自己在某个项目上的历史表现
- Requirement item refs: 功能 4（日/周/月折线 + 8 项指标）
- Acceptance / verification criteria refs: A8（8 项指标含中位数奇偶长度正确）
- Open blocker questions: 无
- Decision: ready
```

```text
Change Necessity:
- User-visible need: 统计 Tab 当前只有占位，功能 4 完全未实现
- No-change / non-code option: 不存在——聚合与图表都必须写代码
- Why code change is necessary: 无既有实现
- Minimum change boundary: 新增 internal/stats 包 + 一个端点 + 前端统计页与折线图；
  不改动 store 的写入路径
- Decision: code-change
```

```text
Existence Check:
- Proposed new surface: ①internal/stats 包 ②/api/projects/:id/stats 端点
  ③前端 line_chart 组件
- Existing owner / reuse candidate: ①store 已有 ListRoundsForUserProject（P3 已交付）；
  ②project 端点已有访问权判定 ③无
- Why existing surface is insufficient:
  ① store 只负责取数，聚合逻辑放进去会让持久化层承担计算职责；
  ② 需要一个新的只读端点；
  ③ Cupertino 无折线图组件
- Creation proof: 规格 §8 定义该子系统；§16 将其列为独立阶段
- Entropy / retirement impact: 无旧路径退役。**明确拒绝**：图表库、
  物化聚合表、时间范围选择器、跨项目汇总
- Decision: add-with-proof
```

```text
Architecture Integrity Lens:
- Invariant: 统计是派生态；只能看自己的数据；统计 Tab 无编辑入口
- Canonical owner / contract: internal/stats 是聚合的唯一 owner，零依赖
- Responsibility overlap: service 只做「访问权 + 取数 + 调用」，不算数
- Higher-level simplification: 用「取回全部轮次、Go 内分桶」代替
  「分桶 SQL + 中位数近似」——个人数据量下前者更简单且不可能算错（规格 §8.1）
- Retirement / falsifier: 若日后有人为了性能加物化表，统计就会与事实源可能不一致
  → 由「无聚合表」的 schema 事实与规格 §8.1 的文字约束防住
- Verdict: 无阻塞问题
```

```text
Plan Pressure Test:
- Owner / contract / retirement: 承载点已识别（分桶、中位数、隐私过滤、无物化）
- Architecture integrity / higher-level path: 已确认
- Verification scope: A8 有专项表驱动测试；隐私边界有 HTTP 层测试
- Task executability: 每步含完整代码与确切命令
- Pressure result: proceed
```

```text
Plan-Time Complexity Check:
- Target files: internal/stats 4 文件；service/api 各 1；前端 4 文件
- Existing size / shape signals: 新包，无既有压力
- Owner fit: 单文件职责清晰（分桶 / 指标 / 编排）
- Add-in-place risk: 若把分桶与指标都放进 stats.go 会混杂两类逻辑；
  且 line_chart 若与统计页同文件会同时承担绘制与页面布局
- Better file boundary: bucket.go / metrics.go / stats.go 分开；
  折线图独立成 widget
- Recommendation: add owner file
```

---

## Task P5.1 —— 分桶

**Files**
- Create: `backend/internal/stats/bucket.go`
- Create: `backend/internal/stats/bucket_test.go`

**Why**：三种粒度的分桶规则是统计的入口。跨月、跨年、时区偏移都在这里出错。

**Change Necessity**：`code-change`。

**Steps**

1. 写 `bucket.go`：

```go
// Package stats 是统计聚合的唯一 owner。
//
// 本包不依赖本仓库其他包，只接受轻量输入（见 Round），因此可独立测试。
//
// 关于浮点：规格 §5.5.3 禁止**判分**路径出现浮点，理由是跨端精确性
// （Go 与 Dart 必须逐例一致）。统计不满足该前提 —— 它只在服务端计算，
// 客户端仅渲染返回值，没有跨端契约。因此本包允许使用 float64：
// 中位数与平均正确率本质是除法，用有理数表示只会增加复杂度而无收益。
package stats

import (
	"fmt"
	"sort"
	"time"
)

// Grain 是时间粒度。
type Grain string

const (
	// GrainDay 按自然日分桶。
	GrainDay Grain = "day"
	// GrainWeek 按星期几（周一…周日）跨日期聚合。
	//
	// 这一粒度回答的是「我哪天最快」，而不是「哪一周最快」——
	// 规格 §8.1 的原话是「按星期几跨日期聚合」。
	GrainWeek Grain = "week"
	// GrainMonth 按自然月分桶。
	GrainMonth Grain = "month"
)

// ParseGrain 解析粒度，非法值返回错误。
func ParseGrain(s string) (Grain, error) {
	switch Grain(s) {
	case GrainDay:
		return GrainDay, nil
	case GrainWeek:
		return GrainWeek, nil
	case GrainMonth:
		return GrainMonth, nil
	default:
		return "", fmt.Errorf("未知的时间粒度 %q（应为 day / week / month）", s)
	}
}

// weekdayOrder 把 time.Weekday 映射为「周一=0 … 周日=6」。
func weekdayOrder(w time.Weekday) int {
	return (int(w) + 6) % 7
}

// bucketKey 返回某个时刻在给定粒度下的桶键，以及排序用的序号。
//
// local 必须已经过时区偏移转换：调用方负责把 UTC 时间转为用户的本地时间。
// 这一点很重要——若按 UTC 分桶，用户在当地 00:30 做的练习会被算到前一天，
// 「今天的练习」在他自己看来就是错的。
func bucketKey(g Grain, local time.Time) (key string, order int, label string) {
	switch g {
	case GrainDay:
		k := local.Format("2006-01-02")
		// 用「距 1970 的天数」排序，避免跨年时字符串比较失效
		return k, int(local.Unix() / 86400), fmt.Sprintf("%d/%d", local.Month(), local.Day())

	case GrainWeek:
		o := weekdayOrder(local.Weekday())
		names := [...]string{"周一", "周二", "周三", "周四", "周五", "周六", "周日"}
		return fmt.Sprintf("dow-%d", o), o, names[o]

	case GrainMonth:
		k := local.Format("2006-01")
		return k, local.Year()*12 + int(local.Month()) - 1, fmt.Sprintf("%d年%d月", local.Year(), local.Month())

	default:
		k := local.Format("2006-01-02")
		return k, int(local.Unix() / 86400), local.Format("2006-01-02")
	}
}

// Bucket 是一个已分组的桶（尚未计算指标）。
type Bucket struct {
	Key   string
	Label string
	Order int
	Rounds []Round
}

// group 按粒度分桶并按时间顺序排序。
//
// 只返回**有数据的桶**：空桶在折线图上会被画成 0，
// 而「那天没练习」与「那天耗时 0 秒」是两回事。
func group(g Grain, rounds []Round, tzOffset time.Duration) []Bucket {
	byKey := map[string]*Bucket{}
	for _, r := range rounds {
		local := r.FinishedAt.UTC().Add(tzOffset)
		key, order, label := bucketKey(g, local)
		b, ok := byKey[key]
		if !ok {
			b = &Bucket{Key: key, Label: label, Order: order}
			byKey[key] = b
		}
		b.Rounds = append(b.Rounds, r)
	}

	out := make([]Bucket, 0, len(byKey))
	for _, b := range byKey {
		out = append(out, *b)
	}
	// 按时间顺序排列（周一在前、日期升序、月份升序）
	sort.Slice(out, func(i, j int) bool {
		if out[i].Order != out[j].Order {
			return out[i].Order < out[j].Order
		}
		return out[i].Key < out[j].Key
	})
	return out
}
```

2. 测试（表驱动）**必须覆盖**：

- `day`：同一自然日归入同一桶；跨日分开
- `day`：**时区偏移**——UTC 2026-09-10T16:30Z 在 `+08:00` 下属于 9/11（次日）
- `month`：跨年（2025-12 与 2026-01 是不同桶且顺序正确）
- `week`：同一天的不同日期归入同一个星期几桶（如两个周三）
- `week`：顺序为周一→周日，与传入顺序无关
- 空输入返回空切片
- 只含有数据的桶（无数据的星期几不出现）
- `ParseGrain` 拒绝非法值

3. 验证：`go test ./internal/stats/ -run TestGroup -v`

4. **Commit**：`feat(stats): 三种粒度的分桶与排序`

---

## Task P5.2 —— 8 项指标与中位数

**Files**
- Create: `backend/internal/stats/metrics.go`
- Create: `backend/internal/stats/metrics_test.go`

**Why**：A8 专门点名中位数。它是 8 项里唯一需要排序且奇偶长度算法不同的一个。

**Change Necessity**：`code-change`。

**Steps**

1. 写 `metrics.go`：

```go
package stats

import "sort"

// Metrics 是一组轮次的 8 项指标。
//
// 按规格 §8.2 的 UI 分成两组（耗时 / 正确率），各 4 项。
// 用独立的字段而非数组，使 JSON 契约自解释，也避免顺序被误解。
type Metrics struct {
	RoundCount int `json:"roundCount"`

	TimeMaxMs    int64   `json:"timeMaxMs"`
	TimeMinMs    int64   `json:"timeMinMs"`
	TimeAvgMs    float64 `json:"timeAvgMs"`
	TimeMedianMs float64 `json:"timeMedianMs"`

	// 正确率为 0..1 的比例，与轮次记录中的 correct_count/question_count 一致。
	AccuracyMax    float64 `json:"accuracyMax"`
	AccuracyMin    float64 `json:"accuracyMin"`
	AccuracyAvg    float64 `json:"accuracyAvg"`
	AccuracyMedian float64 `json:"accuracyMedian"`
}

// Compute 计算一组轮次的 8 项指标。
//
// 空输入返回全零（RoundCount=0），调用方据此判断「无数据」，
// 而不是让平均值出现 NaN。
func Compute(rounds []Round) Metrics {
	if len(rounds) == 0 {
		return Metrics{}
	}

	times := make([]float64, 0, len(rounds))
	accs := make([]float64, 0, len(rounds))

	var timeMax, timeMin int64
	var timeSum, accSum float64
	first := true
	for _, r := range rounds {
		ms := float64(r.TotalMs)
		acc := r.Accuracy()

		times = append(times, ms)
		accs = append(accs, acc)
		timeSum += ms
		accSum += acc

		if first || r.TotalMs > timeMax {
			timeMax = r.TotalMs
		}
		if first || r.TotalMs < timeMin {
			timeMin = r.TotalMs
		}
		first = false
	}

	n := float64(len(rounds))
	return Metrics{
		RoundCount: len(rounds),

		TimeMaxMs:    timeMax,
		TimeMinMs:    timeMin,
		TimeAvgMs:    timeSum / n,
		TimeMedianMs: median(times),

		AccuracyMax:    maxFloat(accs),
		AccuracyMin:    minFloat(accs),
		AccuracyAvg:    accSum / n,
		AccuracyMedian: median(accs),
	}
}

// median 返回中位数。
//
// 必须区分奇偶长度：
//   - 奇数：正中间那个
//   - 偶数：中间两个的**平均**
//
// 忽略这一点会让偶数样本量的中位数系统性偏小（只取了下中位数）。
// 输入会被就地排序——调用方传入的切片都是本函数内部构造的临时切片。
func median(xs []float64) float64 {
	if len(xs) == 0 {
		return 0
	}
	sort.Float64s(xs)
	n := len(xs)
	if n%2 == 1 {
		return xs[n/2]
	}
	return (xs[n/2-1] + xs[n/2]) / 2
}

func maxFloat(xs []float64) float64 {
	m := xs[0]
	for _, x := range xs[1:] {
		if x > m {
			m = x
		}
	}
	return m
}

func minFloat(xs []float64) float64 {
	m := xs[0]
	for _, x := range xs[1:] {
		if x < m {
			m = x
		}
	}
	return m
}
```

2. 测试**必须覆盖**：

| 用例 | 断言 |
|---|---|
| 空输入 | `RoundCount == 0`，全部指标为 0（不出现 NaN） |
| 单元素 | max == min == avg == median == 该值 |
| 奇数长度（3 个） | median 是排序后中间那个 |
| **偶数长度（4 个）** | median 是中间两个的**平均**（这条是 A8 的核心） |
| 偶数长度（2 个） | median 为两者平均 |
| 耗时 max/min | 分别取最大/最小轮次耗时 |
| 正确率 max/min | 分别取最大/最小正确率 |
| 平均正确率 | 各轮正确率的算术平均（**不是**总正确数/总题数） |
| 未排序输入 | 结果与排序后一致（median 内部会排序） |
| 正确率计算 | 3/4 题正确 -> 0.75 |

> 关于「平均正确率」的语义：规格 §8.1 说「对桶内每一轮先取轮正确率，再聚合」，
> 因此是**各轮正确率的平均**，而非「总正确数 / 总题数」。
> 两者在每轮题数相同时相等，题数不同时不同。测试用不同题数固定这一语义。

3. 验证：`go test ./internal/stats/ -run 'TestCompute|TestMedian' -v`

4. **Commit**：`feat(stats): 8 项指标与中位数`

---

## Task P5.3 —— 聚合编排与端点

**Files**
- Create: `backend/internal/stats/stats.go`
- Create: `backend/internal/service/stats.go`
- Create: `backend/internal/api/stats.go`
- Modify: `backend/internal/api/router.go`（接线）
- Create: `backend/internal/api/stats_test.go`

**Why**：把分桶与指标组装成端点契约。

**Change Necessity**：`code-change`。

**Steps**

1. 写 `stats.go`：

```go
package stats

import "time"

// Round 是本包需要的一轮数据。
//
// 刻意使用本包自己的类型而非 store.Round：这样 stats 不依赖任何其它包，
// 调用方负责映射。代价是一次字段拷贝，收益是可独立测试与复用。
type Round struct {
	FinishedAt    time.Time
	TotalMs       int64
	CorrectCount  int
	QuestionCount int
}

// Accuracy 返回本轮正确率（0..1）。
//
// 题数为 0 时返回 0 而非 NaN——后者会污染求和与中位数。
func (r Round) Accuracy() float64 {
	if r.QuestionCount <= 0 {
		return 0
	}
	return float64(r.CorrectCount) / float64(r.QuestionCount)
}

// BucketResult 是一个桶的完整结果。
type BucketResult struct {
	Key   string  `json:"key"`
	Label string  `json:"label"`
	Metrics Metrics `json:"metrics"`
}

// Result 是一次统计查询的结果。
type Result struct {
	Grain    Grain          `json:"grain"`
	Overall  Metrics        `json:"overall"`
	Buckets  []BucketResult `json:"buckets"`
}

// Compute 按粒度分桶并计算 overall 与每桶的 8 项指标。
//
// tzOffset 是用户相对 UTC 的偏移（东八区为 +8h）。
// 分桶在**用户本地时间**下进行：若按 UTC 分桶，当地 00:30 的练习会被算到前一天。
func Compute(g Grain, rounds []Round, tzOffset time.Duration) Result {
	buckets := group(g, rounds, tzOffset)

	out := make([]BucketResult, 0, len(buckets))
	for _, b := range buckets {
		out = append(out, BucketResult{
			Key:     b.Key,
			Label:   b.Label,
			Metrics: ComputeMetrics(b.Rounds),
		})
	}
	return Result{
		Grain:   g,
		Overall: ComputeMetrics(rounds),
		Buckets: out,
	}
}
```

> 命名说明：`metrics.go` 里的函数若叫 `Compute` 会与本文件冲突，
> 实现时统一为 `ComputeMetrics`（或在 metrics.go 中命名 `MetricsFor`）。
> 计划中我写作 `Compute` 是笔误，**以本注为准**。

2. `service/stats.go`：访问权判定（复用 `ProjectAccess`）+ 取数 + 调用 stats：

```go
func (s *ProjectService) Stats(userID, projectID int64, grain stats.Grain, tzOffset time.Duration) (stats.Result, error) {
	// 访问权：必须能访问该项目（作者或订阅者）
	if _, err := s.store.ProjectAccess(userID, projectID); err != nil {
		return stats.Result{}, err
	}
	// 只取**自己**的轮次：作者看不到订阅者的统计，反之亦然
	rows, err := s.store.ListRoundsForUserProject(userID, projectID)
	if err != nil {
		return stats.Result{}, err
	}
	rounds := make([]stats.Round, 0, len(rows))
	for _, r := range rows {
		t, err := time.Parse(time.RFC3339, r.FinishedAt)
		if err != nil {
			// 单条时间损坏不应让整页打不开：跳过它并继续
			continue
		}
		rounds = append(rounds, stats.Round{
			FinishedAt:    t,
			TotalMs:       r.TotalMs,
			CorrectCount:  r.CorrectCount,
			QuestionCount: r.QuestionCount,
		})
	}
	return stats.Compute(grain, rounds, tzOffset), nil
}
```

3. `api/stats.go`：`GET /api/projects/:id/stats`，查询参数：

| 参数 | 必填 | 说明 |
|---|---|---|
| `grain` | 否 | `day`（默认）/ `week` / `month` |
| `tzOffsetMinutes` | 否 | 相对 UTC 的分钟偏移（东八区为 480），默认 0 |

校验：`tzOffsetMinutes` 必须在 `[-840, 840]`（±14 小时）内，否则 400。

4. 测试（api 层）**必须覆盖**：

- 三种粒度都能返回 200 且 `grain` 回显正确
- 非法 `grain` -> 400 `bad_request`
- `tzOffsetMinutes` 越界 -> 400
- 无关用户请求 -> 403
- 未登录 -> 401
- 不存在的项目 -> 404
- **隐私边界**：作者建的共享项目，订阅者做完练习后，
  作者取统计**不应包含**订阅者的轮次（`overall.roundCount` 为作者自己的数量）
- 无任何练习记录时返回 `overall.roundCount == 0` 且 `buckets` 为空数组（非 null）

5. **Commit**：`feat(api): 统计端点（日/周/月分桶 + 8 项指标）`

---

## Task P5.4 —— 统计页与折线图

**Files**
- Create: `app/lib/api/stats_api.dart`
- Create: `app/lib/state/stats.dart`
- Create: `app/lib/ui/widgets/line_chart.dart`
- Modify: `app/lib/ui/stats_tab.dart`（替换占位）
- Create: `app/test/ui/line_chart_test.dart`

**Why**：功能 4 的界面。

**Change Necessity**：`code-change`。

**Impact / Compatibility**：**统计 Tab 不得出现任何编辑入口**（规格 §4.3）。

**Steps**

1. `stats_api.dart`：模型 `StatsResult` / `BucketStats` / `Metrics`（8 项），
   调用时带上 `grain` 与 `tzOffsetMinutes: DateTime.now().timeZoneOffset.inMinutes`。

2. `state/stats.dart`：`StatsNotifier` 持有「当前项目 + 粒度」，切换时重新拉取；
   暴露 loading / error / result。

3. `line_chart.dart`：`CustomPainter` 自绘单条折线。

要求：

- 只画**一条**线（规格：绝不双 Y 轴）
- y 轴范围按数据自适应，并在上下各留 10% 余量；
  所有值相同时给一个固定跨度，避免除零
- x 轴按桶顺序等距分布（**不按时间比例**——周的粒度下时间轴没有意义）
- 少于 2 个点时画点不画线
- 无数据时显示占位文案而非空白
- 精度：正确率显示为百分比，耗时显示为秒

4. `stats_tab.dart`：

```
项目选择器（CupertinoSlidingSegmentedControl 或列表）
粒度 [日][周][月]        指标 [耗时][正确率]
折线图（单条线）
耗时指标卡          正确率指标卡
  最高/最低           最高/最低
  平均/中位           平均/中位
```

- 数据来源为 `overall` 的 8 项指标
- 折线图使用每桶的 `metrics.timeAvgMs` 或 `metrics.accuracyAvg`
- 无数据时提示「还没有练习记录」
- **不放置任何编辑入口**（无新建、无编辑、无删除）

5. 测试：

- `LineChartPainter` 在空数据、单点、多点下均不抛异常
- 统计页在无数据时显示提示文案
- y 轴范围：全等值时不除零

6. **Commit**：`feat(app): 统计页与自绘折线图`

---

## Task P5.5 —— P5 验收

**Files**：无新增（验证与证据）

1. 全量验证并逐条核对 A8，写入
   `docs/aegis/work/2026-09-10-mental-math-app/evidence/p5/ACCEPTANCE.md`：

| 标准 | 证明它的确切命令 |
|---|---|
| A8 8 项指标含中位数（奇偶长度）正确 | `go test ./internal/stats/ -run TestMedian -v` 与 `-run TestCompute -v` |
| 三种粒度分桶正确（含时区、跨年） | `go test ./internal/stats/ -run TestGroup -v` |
| 隐私边界 | `go test ./internal/api/ -run TestStatsIsPrivate -v` |
| 统计为派生态（删记录后统计消失） | 端到端：做一轮 → 查统计 → 删项目 → 项目不存在 |
| 统计 Tab 无编辑入口 | 人工检查 + 代码中无写操作调用 |

2. **手工端到端**：建项目 → 做 3 轮（不同正确率）→ 取三种粒度统计并核对数字。

3. **变异测试取证**：把中位数的偶数分支改成「取下中位数」，
   确认 A8 的测试失败；还原。

4. 更新工作记录并提交。

5. **Commit**：`test(stats): P5 验收记录`

---

## Risks

| # | 风险 | 处置 |
|---|---|---|
| R-P5-1 | 中位数偶数分支写错（取下中位数） | 表驱动测试含 2/4 个元素；并有变异测试取证 |
| R-P5-2 | 按 UTC 分桶导致「今天的练习算到昨天」 | 端点接受 `tzOffsetMinutes`；测试覆盖 UTC 16:30 在 +08:00 下属于次日 |
| R-P5-3 | 平均正确率语义混淆（各轮平均 vs 总正确率） | 按规格 §8.1 取「各轮正确率的平均」；测试用不同题数固定该语义 |
| R-P5-4 | 作者看到订阅者的统计（隐私） | 按 `(user_id, project_id)` 过滤；HTTP 层测试守护 |
| R-P5-5 | 有人为性能加物化聚合表 | 规格 §8.1 与基线 §5.2(4) 已禁止；统计无 schema |
| R-P5-6 | 大量历史数据导致分桶数量过多 | 个人练习数据量下可忽略；若日后成为问题，加时间范围参数而非物化表 |

## Retirement

- **Old owner / fallback**：无。全部新增。
- **Deletion trigger**：无。
- 明确拒绝：图表库、物化聚合表、时间范围选择器、跨项目汇总。

## ADR Signals

本阶段是 **ADR-3「统计为派生态而非物化态」** 的落地。P5 完成后可回填：
三种粒度的定义、每桶 8 项指标、时区处理、以及「统计允许浮点而判分不允许」的
边界理由。

---

## Execution Readiness View

```text
Execution Readiness View:
- Intent Lock: 交付功能 4（日/周/月折线 + 8 项指标）
- Scope Fence: 不含跨项目汇总、导出、时间范围选择器、订阅（P6）
- Baseline Lock: 规格 §8.1/§8.2/§4.3/§6.1(5)；基线 §5.2(4)、§9(4)
- Approved Behavior: 统计由 round 实时派生；只统计本人；统计 Tab 只读
- Owner / Contract Constraints: internal/stats 是聚合唯一 owner 且零依赖；
  service 只做访问权 + 取数
- Compatibility Boundary: 不新增聚合表；不改动 round 写入语义；统计 Tab 无编辑入口
- Retirement Boundary: 无退役对象；拒绝图表库与物化表
- Task Batches: P5.1(分桶) → P5.2(指标) → P5.3(编排+端点) → P5.4(页面) → P5.5(验收)
- Test Obligations: 分桶 8 项（含时区/跨年/星期顺序）、中位数奇偶、8 项指标语义、
  端点校验 6 项、隐私边界、折线图边界
- Review Gates: 每 Task 后 go build/vet/test 或 analyze/test；P5.5 变异测试
- Drift / Rewind Rules: 若需要新增表或改动写入语义，停止并回到规格 §8
- Evidence Required Before Completion: 全量测试输出；A8 专项；
  端到端三种粒度数字核对；中位数变异测试
- Advisory Boundary: method-pack execution guidance only; not GateDecision,
  PolicySnapshot, or completion authority
```

## Execution Route

```text
Execution Route:
- Decision: inline
- Evidence: P5.1→P5.2→P5.3 严格顺序依赖（分桶 → 指标 → 编排），
  且共享同一包；P5.4 依赖 P5.3 的契约
- Fallback: 无
- User confirmation required: no
```

**REQUIRED SUB-SKILL**：aegis:executing-plans
